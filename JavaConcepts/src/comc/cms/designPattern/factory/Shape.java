/*
 * In factory pattern we created the object without exposing the creation logic to the client
 * and refer to the newely created object using the common interface
 *  */
package comc.cms.designPattern.factory;

public interface Shape {
	public void draw();
}
